# Learning log

Vacancy Chennai engineering notes (pitfalls, SEO patterns, Next.js conventions) are maintained here:

**[vacancychennai/docs/LEARNING.md](vacancychennai/docs/LEARNING.md)** — includes **auth email** (Resend, employer verification, candidate magic link, tokens, env), **admin password reset**, **scheduled job digests + cron + Twilio SMS**, **Next.js 16 `proxy.ts`** (replaces deprecated `middleware`), **repository vs mock DB** (audits, API parity), and **hyperlocal `[locationPage]`** validation (avoid loose zone substring matches).

Pair with [vacancychennai/docs/PROJECT_UPDATE.md](vacancychennai/docs/PROJECT_UPDATE.md) (changelog) and [vacancychennai/AGENTS.md](vacancychennai/AGENTS.md) (agent rules).
